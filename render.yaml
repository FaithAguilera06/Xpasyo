services:
  - type: web
    name: xpasyo
    rootDir: web
    env: php
    buildCommand: composer install --no-dev --optimize-autoloader
    startCommand: php -S 0.0.0.0:10000 -t .
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
    httpPort: 10000
    healthCheckPath: /pages/INDEX.php
